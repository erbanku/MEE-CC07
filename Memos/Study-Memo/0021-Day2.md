```
import RPi.GPIO as GPIO
import time
GPIO.setmode(GPIO.BCM)
GPIO.setup(25,GPIO.OUT)
while True:
	GPIO.output(25,GPIO.HIGH)
	time.sleep(1)
	GPIO.output(25,GPIO.LOW)
	time.sleep(1)
```

以上为闪烁的原理。

```
import RPi.GPIO as GPIO
import time
GPIO.setmode(GPIO.BCM)
GPIO.setup(24,GPIO.IN)
GPIO.setup(25,GPIO.OUT)
GPIO.setup(23,GPIO.OUT)
a =0
while True:
    inputValue = GPIO.input(24)
    if (inputValue == True):
        a= a+1
        print(a)
    if (a % 4 == 1):
        GPIO.output(23,GPIO.HIGH)
        GPIO.output(25,GPIO.LOW)
        time.sleep(0.5)
    elif(a % 4 == 2):
        GPIO.output(23,GPIO.HIGH)
        GPIO.output(25,GPIO.HIGH)
        time.sleep(0.5)
    elif(a % 4 == 3):
        GPIO.output(25,GPIO.HIGH)
        GPIO.output(23,GPIO.LOW)
        time.sleep(0.5)
    else:
        GPIO.output(25,GPIO.LOW)
        GPIO.output(23,GPIO.LOW)
        time.sleep(0.5)
    

```

以上为计数器的原理。

```
import RPi.GPIO as GPIO
import time
GPIO.setmode(GPIO.BCM)
GPIO.setup(24,GPIO.IN)
GPIO.setup(25,GPIO.OUT)
while True:
	inputValue = GPIO.input(24)
	if (inputValue == True):
		GPIO.output(25,GPIO.HIGH)
		time.sleep(1)
	else:
		GPIO.output(25,GPIO.HIGH)
		time.sleep(1)
```

以上为按键的原理。

小结：

1.初步了解了树莓派这一强大的硬件。

2.使用简单的python语言进行比较简单的编程。

3.使用面包板进行了简要的电路的连接。

4.总体感觉比较好（相比于上次课），了解了python语言的最基本的套路，谢谢老师的辅导。